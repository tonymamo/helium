{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    },
    {
      "src": "api/vercel_handler.py",
      "use": "@vercel/python",
      "config": {
        "buildCommand": "pip install --upgrade pip && pip install -r requirements.txt",
        "installCommand": "apt-get update && apt-get install -y build-essential cmake"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/vercel_handler.py"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
